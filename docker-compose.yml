services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # DATABASE_URL is needed at build time for Prisma + Next.js static analysis
        DATABASE_URL: ${DATABASE_URL}
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped